---
description: Logging standards - level definitions, readability and usefulness
alwaysApply: true
---

# 日志规范

## 级别定义

| 级别 | 含义 | 标准 |
|------|------|------|
| `error` | 半夜打电话叫醒我 | 仅用于：支付/订单数据丢失、事务提交失败等影响用户资金或数据完整性的问题 |
| `warn` | 第二天上班看 | 有异常/不对的事情发生：数据库断连、客户端参数异常、验证失败、外部服务失败等。偶发可能是客户端问题，频发可能是 bug |
| `info` | 一切正常 | 正常运营流水：请求处理、连接建立、服务启动等 |

- 新代码只用 `info` / `warn` / `error`，禁止 `debug` / `trace`。
- 修改旧代码时顺手把 `debug`/`trace` 提升为 `info`。
- 日志消息中禁止 emoji，保持纯文本。

### error 准入门槛

写 `error!` 之前自问：**这条日志值得半夜把我叫醒吗？** 如果答案是"不"，用 `warn!`。

只有以下场景才用 error：
- 用户付了钱但许可证/购买记录创建失败
- 数据库事务提交或回滚失败（数据一致性破坏）
- 退款处理发现数据异常

以下场景**必须用 warn 而不是 error**：
- 数据库临时断连（会自动重连）
- 客户端发了无效参数/垃圾数据
- Apple/Stripe 验证失败（客户端问题或重放攻击）
- Telegram 通知发送失败（不影响核心业务）
- 设备数量超限被拒绝（正常业务逻辑）
- 外部 API 调用超时或失败
